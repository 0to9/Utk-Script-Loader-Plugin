// beware lack of auto boxing/unboxing
// beware lack of parameterization/generics
// beware lack of varargs/variable arguments
/* also you can use multiline comments */

#include me "test.txt"

#ref Map_Entry java.util.Map.Entry

import java.util;

Map map = new HashMap();

@Init : {
    for (int i = 0; i < 10; i++)
        map.put("" + i, "" + i);

    for ($Map_Entry e : map.entrySet())
        System.out.println(e.getKey() + "->" + e.getValue());
}

struct Obj {
    int val;

    init Obj(int val) {
        this.val = val;
    }
}